#include <iostream>
#include <math.h>
using namespace std;

int count_eight(char* num)
{
    int cnt = 0;

    for(int i=0; num[i]; i++)
    {
        if (num[i]=='8')
            cnt++;
    }
    return (cnt);
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    char *L, *R;
    int result = 0;
    char *now;

    for(int j = atoi(L); j <= atoi(R); j++)
    {
        now = itoa
        
        
    }    


    return (0);
}